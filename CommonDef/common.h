#ifndef __COMMON_H__
#define __COMMON_H__

#include <string>
#include <map>
#include <iostream>
#include <string.h>

using namespace std;

#define Int64 __int64

#define Between(X, L, H) (((X) >= (L) && (X) <= (H)))


#endif